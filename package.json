{
  "name": "github-trending-daily",
  "version": "1.0.0",
  "private": true,
  "description": "Daily GitHub Trending digest archived and published to GitHub Pages",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "fetch": "node scripts/fetch.js",
    "generate": "node scripts/generate.js",
    "build": "node scripts/generate.js",
    "update": "node scripts/update.js",
    "test": "node --test test/*.test.js",
    "preview": "node scripts/generate.js && npx --yes serve site -l 4173"
  },
  "dependencies": {
    "cheerio": "1.1.2"
  }
}
